@AGENTS.md

# 응답 언어 규칙

이 프로젝트(tutoring-site)에서 사용자에게 보여주는 모든 응답과 작업 진행 설명은 한국어로 작성한다.

- 최종 답변은 한국어로 작성한다.
- 작업 중간 진행상황 설명도 한국어로 작성한다.
- 오류 설명도 한국어로 작성한다.
- 보고서 요약도 한국어로 작성한다.
- 코드, 파일명, 명령어, 고유 기술용어는 영어 그대로 사용해도 된다.
- 사용자가 영어 답변을 명시적으로 요청한 경우에만 영어를 사용한다.

# 작업 효율 및 토큰 최적화

목표: 품질·정확도는 유지하면서 불필요한 모델 호출·컨텍스트·도구 사용만 줄인다. 필요한 검증·분석은 생략하지 않는다.

1. 작업 시작은 `git status`/`diff`/`log`/`rg`로 관련 파일만 파악한다. 전체 프로젝트 재탐색 금지.
2. 변경 없는 파일, 기존 QA(`docs/qa/`), 이미 확인한 validator 결과는 재조사하지 않는다.
3. 큰 파일은 rg로 위치를 찾은 뒤 필요한 범위만 읽는다.
4. 파일 1~5개 탐색/수정은 직접 처리한다. Subagent는 독립적 작업이 2개 이상이고 병렬화로 실제 이득이 있으며 중복 조사가 없을 때만 사용한다.
5. 검증은 수정 범위 → 관련 테스트 → lint/typecheck → build 순으로 점진적으로 수행하고, 작은 수정마다 전체를 반복하지 않는다.
6. 전체 crawl/Playwright/반응형 전수검사는 공통 구조 변경 또는 릴리즈 체크포인트에서만 수행한다.
7. UI 변화 없는 metadata/SEO/설정 변경은 스크린샷 검수를 생략한다.
8. 반복되는 검사는 기존 validator/script로 자동화하고 결과를 짧게 요약 출력한다(예: `Build: PASS`, `Sitemap: 60`). 정상 raw output은 재분석하지 않고 실패 항목만 상세 분석한다.
9. `git log`/`show`/`diff`, `docs/qa`, README를 작업 기억장치로 활용하고 이미 커밋된 완료 작업은 재조사하지 않는다.
10. 진행 메시지와 최종 보고는 변경사항·검증 결과·실패 항목 중심으로 짧게 작성한다(성공 로그 전문, 전체 diff 설명, 읽은 파일 목록은 생략).
11. 로컬 코드로 판단 가능하면 새 패키지·외부 검색·MCP를 사용하지 않는다.
12. 보안, 개인정보, 결제, 배포, 데이터 삭제, Git history 변경, SEO index/noindex 대규모 변경, 전체 라우팅 변경, Production 환경에서는 효율보다 정확성을 우선하며 필요한 검증을 생략하지 않는다.
13. 추가 검사가 새 문제를 발견할 가능성이 낮으면 반복 검증을 종료한다.
14. 완전히 다른 작업으로 전환할 때는 새 세션/`/clear`를, 같은 복잡한 작업을 이어갈 때는 `/compact`를 짧게 안내한다.

충돌 시 12번(정확성/안전성)을 우선한다.
